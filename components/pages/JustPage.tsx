"use client"

export default function JustPage() {
  return (
    <div style={{ background: "#000", color: "#fff", minHeight: "100vh", display: "flex", alignItems: "center", justifyContent: "center" }}>
      <h1 style={{ fontFamily: "sans-serif" }}>OK — page chargée</h1>
    </div>
  )
}